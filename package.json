{
  "name": "app-review-analyser",
  "private": true,
  "description": "Monorepo: Python pipeline + Next.js UI (workspace `pulse-ui`).",
  "workspaces": ["phase6_ui"],
  "dependencies": {
    "next": "14.2.35",
    "react": "^18",
    "react-dom": "^18"
  },
  "scripts": {
    "build": "npm run build -w pulse-ui",
    "dev": "npm run dev -w pulse-ui"
  }
}
